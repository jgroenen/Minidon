<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # Stuur alle requests naar index.php, behalve bestaande bestanden/mappen
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php [QSA,L]
</IfModule>